ALTER TABLE push_booster.subscribers
    ADD COLUMN IF NOT EXISTS ip String AFTER referrer,
    ADD COLUMN IF NOT EXISTS country String AFTER ip,
    ADD COLUMN IF NOT EXISTS region String AFTER country,
    ADD COLUMN IF NOT EXISTS city String AFTER region,
    ADD COLUMN IF NOT EXISTS timezone String AFTER city,
    ADD COLUMN IF NOT EXISTS language String AFTER timezone,
    ADD COLUMN IF NOT EXISTS browser_name LowCardinality(String) AFTER language,
    ADD COLUMN IF NOT EXISTS browser_version String AFTER browser_name,
    ADD COLUMN IF NOT EXISTS os_name LowCardinality(String) AFTER browser_version,
    ADD COLUMN IF NOT EXISTS os_version String AFTER os_name,
    ADD COLUMN IF NOT EXISTS device_type LowCardinality(String) AFTER os_version,
    ADD COLUMN IF NOT EXISTS device_vendor String AFTER device_type,
    ADD COLUMN IF NOT EXISTS device_model String AFTER device_vendor,
    ADD COLUMN IF NOT EXISTS ua_platform String AFTER device_model,
    ADD COLUMN IF NOT EXISTS ua_platform_version String AFTER ua_platform,
    ADD COLUMN IF NOT EXISTS ua_mobile UInt8 AFTER ua_platform_version,
    ADD COLUMN IF NOT EXISTS ua_full_version String AFTER ua_mobile,
    ADD COLUMN IF NOT EXISTS ua_arch String AFTER ua_full_version,
    ADD COLUMN IF NOT EXISTS ua_bitness String AFTER ua_arch;
